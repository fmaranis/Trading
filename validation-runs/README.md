# Replay runtime results

This directory contains locally saved historical replay audit artifacts. `latest.json` is the full local audit. A compact `latest-chatgpt.json` may be published automatically to the dedicated `replay-results` branch when server-side GitHub replay sync is configured.
