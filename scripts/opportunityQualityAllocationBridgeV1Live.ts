// Compatibility entry point kept for the existing ResearchValidationCenter job.
// The first QUALITY_ALLOCATION_BRIDGE_V1 run is consumed. The current run only
// expands diagnostics to locate where the frozen bridge loses economic reach.
import './opportunityAllocationConstraintAuditV1Live';
